{
	"name": "ai-scraper",
	"version": "1.0.0",
	"description": "Web application for extracting and analyzing website content using OCR, web scraping, and AI",
	"main": "index.js",
	"scripts": {
		"start": "concurrently \"npm run backend\" \"npm run frontend\"",
		"backend": "cd backend && npm run dev",
		"frontend": "cd frontend && npm run dev",
		"install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
	},
	"keywords": [
		"ocr",
		"web-scraping",
		"ai",
		"llm",
		"tesseract",
		"puppeteer",
		"react"
	],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"concurrently": "^8.2.2"
	}
}
